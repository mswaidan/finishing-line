 Program
   BeforeStart
     Set ROBOT_SENDING= False 
     Script: set_conveyor_params.script
     Script: set_motion_mode.script
     Script: position_move.script
     Script: distance_move.script
     Script: mm_move.script
     Script: continuous_move.script
     Script: idle_move.script
     Script: request_id.script
     Script: steps_per_inch.script
     Script: mm_to_steps.script
     Set_Conveyor_Params(1600, 16000)
     Move_Idle()
     Script: select_job.script
     loaded≔0
     dried≔False
     Set BRUSH_ON= False 
     'MoveJ'
   Robot Program
     MoveJ
       Sand_Base
     'Loop RUN==False'
     'Load Workpiece'
     'If RUN == True'
     Motion Test
       Conveyor_Move_mm(width)
       Wait SERVER_STATE == 1
       'Wait: 2.0'
     sync()
   Thread_1
     If RUN == False
       If MOTION_MODE ≠3
         Move_Idle()
       If FEED_CONVEYOR≟ True 
         Set FEED_CONVEYOR= False 
       sync()
     Else
       sync()
   Thread_2
     If timer_brush > 30
       Set BRUSH_ON= False 
       timer_brush: Stop
       timer_brush: Reset
     sync()
